{
  "scripts": {
    "start": "node server.mjs",
    "dev": "node server.mjs",
    "a3:agent": "node tools/a3-local-agent.mjs",
    "controljus:fetch": "node tools/controljus-fetch.mjs",
    "build:cloudflare": "node tools/build-cloudflare.mjs",
    "dev:cloudflare": "npm run build:cloudflare && wrangler dev",
    "deploy:cloudflare": "npm run build:cloudflare && wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/puppeteer": "^1.1.0",
    "playwright": "^1.61.1",
    "wrangler": "^4.107.0"
  }
}
